package org.aksw.jena_sparql_api.schema.traversal.state.l3;


// A stateless variant of the Trav - feeding path segments to the processor rotates
// the state between the given types
class StateRotation3 {
    /*
    interface StateRotation<T, S> {
        StateRotationnext(T segment);
    }

    public interface StateRotation3Provider<T, S, A extends S, B extends S, C extends S> {
        A mkRoot();
        B toB(A a, T segment);
        C toC(B a, T segment);
        A toA(C a, T segment);
    }

    class State
    */
}