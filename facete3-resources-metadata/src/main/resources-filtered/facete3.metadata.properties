# Generic metadata - if desired we could track versions/build times on a
# per-module basis.
# Especially during development usually only one only builds a subset of the modules
facete3.version=${project.version}
facete3.build.timestamp=${timestamp}

